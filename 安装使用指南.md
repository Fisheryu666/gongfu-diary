# 功夫日记 · 安装使用指南

一个本地部署的手机小应用：每天记录明心净心的功夫 —— 人生志向、十不、读书学习、反省改过、今日行动、当日总结，并用「引导24时」重新规划一天的时间，与志向目标相应。

**数据完全保存在手机本机**（浏览器本地存储），不联网、不上传，可离线使用。

> 🌐 **线上地址（GitHub Pages 免费托管，已部署）**：https://fisheryu666.github.io/gongfu-diary/
> 手机浏览器打开这个网址，按下面第三节「添加到主屏幕」即可使用。HTTPS 环境，语音输入可用。
> 源码仓库：https://github.com/Fisheryu666/gongfu-diary

---

## 一、功能说明

| 页面 | 内容 |
|---|---|
| 日记 | ① 倾诉我的志向（一段）② 十不（一个框写十条戒律）③ 读书学习（一条）④ 反省改过（一条）⑤ 今日行动（一条）⑥ 当日总结 |
| 24时 | 早上 5:00 到晚上 22:00 共九个时段，每段三行：原本生活写照 → 2.0 重新规划 → 3.0 升级生活 |
| 搜索 | 快速查看最近 6 天的记录；更早的历史只需选择日期查询（选一个日期查当天，选起止两个查时间段），点击结果直达当天；页面底部可导出 / 导入备份 |

- **连续填写天数**：日记页日期下方显示「已连续填写 N 天」，每天坚持记录，数字就会累积。
- **语音输入**：每个输入框右侧有麦克风按钮，点一下开始说话，说完点红色按钮停止，文字自动填入。
- **自动保存**：边写边存，无需手动操作；顶部显示「已完成 x / 6 项」。
- **切换日期**：日记页顶部 ‹ › 可翻看历史日期，随时补记、回看。

---

## 二、部署方式（已完成 ✅）

应用已部署到 GitHub Pages：**https://fisheryu666.github.io/gongfu-diary/**

这是一个纯静态网页应用，三种手机共用同一份代码，无需分别开发。以后如果修改了代码，更新线上版本的步骤：

```bash
cd gongfu-diary
npm run build              # 重新生成 dist/
rm -rf docs && cp -r dist docs   # 同步到 docs/（Pages 托管目录）
git add -A && git commit -m "更新" && git push
# 推送后约 1 分钟，线上自动更新
```

> 注意：语音输入（麦克风权限）要求 **HTTPS** 环境，GitHub Pages 已满足。

---

## 三、三个平台的安装（添加到主屏幕，像原生 App 一样使用）

### 华为鸿蒙 HarmonyOS 版

1. 用**华为浏览器**打开部署好的网址。
2. 点右下角 **⋮ 菜单 → 添加至桌面**（或「更多 → 添加书签 → 桌面」）。
3. 桌面出现红色「功」字图标，点开即全屏使用。
4. 语音输入：首次点麦克风时允许麦克风权限即可（鸿蒙浏览器基于 Chromium，语音识别支持良好）。

### 安卓 Android 版

1. 用 **Chrome**（或系统浏览器）打开网址。
2. 点右上角 **⋮ → 添加到主屏幕 / 安装应用**。
3. 若地址是 HTTPS，Chrome 会直接提示「安装功夫日记」，安装后在应用列表里有独立图标，离线也能打开。
4. 语音输入：允许麦克风权限后使用，识别走系统/Google 语音服务。

### iPhone 版

1. 用 **Safari** 打开网址（必须 Safari，才能添加到主屏幕）。
2. 点底部**分享按钮 → 添加到主屏幕**。
3. 桌面出现「功夫日记」图标，点开全屏使用。
4. 语音输入：iOS 17 及以上 Safari 支持网页语音识别；如果点麦克风无反应，请升级到最新 iOS，或直接用键盘自带的语音转文字（键盘右下角麦克风），效果同样好。

---

## 四、日常使用建议

- **早上**：打开「日记」页，先写志向，对照「十不」，再写学习内容和今日行动；打开「24时」页对照志向重排今天的时间。
- **晚上**：补写反省改过和当日总结。
- **随时**：在「搜索」页输入关键词（如「君子」「抖音」「儿子」），快速找到所有相关记录。

## 五、数据备份（导出 / 导入）

数据存在手机浏览器本地，换手机或清除浏览器数据前请备份：

1. **导出**：打开「搜索」页，滑到底部「数据备份」，点 **导出全部数据**，会下载一个 `功夫日记备份-日期.json` 文件，保存到微信文件传输助手 / 网盘 / 电脑均可。
2. **导入**：在新手机装好应用后，点 **导入备份文件**，选择之前导出的 JSON 文件即可。
3. **合并规则**：同一天的数据以较新者为准，不会覆盖掉更新的记录，也不会丢数据。
